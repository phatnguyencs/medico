import os, sys
import numpy as np
import torch



def do_crf_inference(image, pred, cfg):
    '''
    Args:
        image: np.array of shape [batch, 3, H, W]
        pred: np.array of shape [batch, n_class, H, W]
    '''


    pass
